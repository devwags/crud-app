describe('header component', () => {
    test('renders a home button', () => {
        //test code here
    }),
    test('when clicked, the home button routes user to the root path and displays the visitor inventory', () => {
        //test code here
    }),
    test('when user is not logged in, the header component renders a login button', () => {
        //test code here
    })
})