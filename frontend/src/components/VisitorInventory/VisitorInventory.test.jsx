describe('inventory component', ()=>{
    test('when user is not logged in, display all items', () => {
        //test code here
    }),
    test('when user is logged in, render cards for each item associated with their userId', () => {
        //test code here
    }),
    test('when user is logged in, render an add element icon', () => {
        //test code here
    })
})